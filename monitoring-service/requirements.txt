# For HTTP requests to backend
requests==2.32.5

# For pinging devices
ping3==4.0

# For scheduling tasks
schedule==1.2.0

# Optional: for logging with timestamps in a nice format
python-dateutil==2.9.0

# Optional: if you ever expand to async or web APIs
aiohttp==3.9.5
